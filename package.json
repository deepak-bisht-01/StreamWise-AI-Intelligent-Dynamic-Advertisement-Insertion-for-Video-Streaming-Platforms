{
  "name": "lumina",
  "version": "1.0.0",
  "private": true,
  "description": "Lumina video library — client and API",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
